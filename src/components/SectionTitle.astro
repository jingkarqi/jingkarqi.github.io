---
interface Props {
  title: string;
  description?: string;
  linkHref?: string;
  linkLabel?: string;
  eyebrow?: string;
}

const { title, description, linkHref, linkLabel, eyebrow } = Astro.props;
---

<div class="section-header">
  <div>
    {eyebrow && <p class="section-eyebrow">{eyebrow}</p>}
    <h2>{title}</h2>
    {description && <p>{description}</p>}
  </div>
  {linkHref && linkLabel && (
    <a class="section-link" href={linkHref}>
      {linkLabel}
    </a>
  )}
</div>
